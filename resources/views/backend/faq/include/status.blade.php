<a class="badge tooltip-top bg-{{ $faq['status'] == '1' ? 'success' : 'danger' }} view-btn faq-status" data-id="{{ $faq['status'] }}" role="button" data-userId="{{ $faq['id'] }}" data-url="{{ route('faq.status', $faq['id']) }}" data-tooltip="Click to {{ $faq['status'] == '1' ? 'Inactive' : 'Active' }}"><span class="text-white">{{ $faq['status'] == '1' ? 'Active' : 'Inactive' }}</span></a>
