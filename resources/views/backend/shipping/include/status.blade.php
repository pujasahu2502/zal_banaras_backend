<a class="badge tooltip-top bg-{{ Str::slug(collect($shipping)->first()->status) == '1' ? 'success' : 'danger' }} view-btn shipping-status" data-id="{{  Str::slug(collect($shipping)->first()->zone_name) }}" role="button" data-userId="{{  Str::slug(collect($shipping)->first()->zone_name) }}" data-url="{{route('shipping.status', Str::slug(collect($shipping)->first()->zone_name) )}}" data-tooltip="Click to {{  Str::slug(collect($shipping)->first()->status) == '1' ? 'inactive' : 'active' }}"><span class="text-white">{{  Str::slug(collect($shipping)->first()->status) == '1' ? 'Active' : 'Inactive' }}</span></a>