<span class="badge tooltip-top bg-{{ $listing['status'] == '1' ? 'success' : 'danger' }} view-btn listing-status" data-id="{{ $listing['status'] }}" data-url="{{ route('listing.status',$listing['id']) }}"   role="button" data-userId="{{ $listing['id'] }}" data-name="{{ $listing['name'] ?? ''  }}" data-tooltip="Click to {{ $listing['status'] == '1' ? 'Inactivate' : 'Activate' }} "><span class="text-white">{{ $listing['status'] == '1' ? 'Active' : 'Inactive' }}</span></a></span>